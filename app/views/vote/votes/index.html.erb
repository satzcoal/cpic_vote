<h5 class="widget-name"><i class="fa fa-edit"></i>所有投票</h5>
<div class="navbar">
  <div class="navbar-inner">
    <h6>投票列表</h6>
    <%= link_to '新建投票', new_vote_vote_path, :class => 'btn btn-success pull-right' %>
  </div>
</div>
<% if @votes.empty? %>
  <div class="alert">
    暂时还没有投票，请新建投票！
  </div>
<% else %>
  <table class="table table-striped table-bordered table-condensed table-hover">
    <thead>
    <tr>
      <th>名称</th>
      <th>状态</th>
      <th>开始时间</th>
      <th>截止时间</th>
      <th>操作</th>
    </tr>
    </thead>
    <tbody>
    <% @votes.each do |v| %>
      <tr>
        <td><%= v.name %></td>
        <td><%= Vote::Vote.get_status v.status %></td>
        <td><%= v.start_time.localtime.strftime('%Y-%m-%d %H:%M:%S') %></td>
        <td><%= v.stop_time.localtime.strftime('%Y-%m-%d %H:%M:%S') %></td>
        <td><%== vote_index_op_buttom v %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% end %>